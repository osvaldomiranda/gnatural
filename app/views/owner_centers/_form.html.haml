= form_for @owner_center do |f|
  - if @owner_center.errors.any?
    #error_explanation
      %h2= "#{pluralize(@owner_center.errors.count, "error")} prohibited this owner_center from being saved:"
      %ul
        - @owner_center.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :user
    = f.text_field :user
  .field
    = f.label :rut_centro
    = f.text_field :rut_centro
  .actions
    = f.submit 'Save'
